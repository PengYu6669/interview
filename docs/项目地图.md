# InterviewCopilot 项目地图

> 只记录当前真实实现。核心链路、事实来源或外部服务变化时更新，不作为开发日志。

## 一句话说明

面向技术岗位候选人的学习与训练 Web：从个人材料、岗位要求和公共知识中检索证据，支持题库学习、真实模拟面试、结构化表达、业务 Sense 训练，并把有原句证据的结果沉淀到成长档案。

## 运行边界

```text
Browser -> Next.js Web/BFF -> FastAPI
                            |- deterministic services
                            |- bounded training Agent / LangGraph
                            |- RAG indexing and retrieval
                            |- DeepSeek / Doubao Embedding / Baidu OCR / speech provider
                            `- PostgreSQL + pgvector
```

- Next.js 负责页面、浏览器状态、登录展示和 BFF，不保存厂商密钥。
- FastAPI 负责文档、检索、训练编排、评价、语音票据和厂商适配。
- PostgreSQL 是账号、会话、回答、报告、评价和审计的事实来源；Redis 只承担登录失败限流等可丢失的协调状态，不是核心事实来源。
- 不保存原始录音；语音识别文本由用户确认提交后才作为回答保存。

## 当前核心链路

### 学习中心

- 入口：`/questions`、`/questions/[slug]`。
- 调用链：Next BFF -> `api/questions.py` -> `QuestionWorkflowService` -> PostgreSQL；带引用问答通过混合 RAG 检索所选题目，再调用 DeepSeek。
- 状态与事实来源：题目、Markdown、来源、学习状态和聊天记录均在 PostgreSQL。
- 失败表现：解析、检索或模型失败返回中文错误，不伪造答案或引用。

### 结构化表达与业务 Sense

- 入口：`/training/new?mode=...` -> `/training/[sessionId]`。
- 调用链：BFF -> `api/coaching.py` -> `CoachingService` -> `TrainingCoachAgent` -> versioned Skill/rubric -> DeepSeek JSON；只有用户明确选择资料时，出题阶段才开放 candidate/job/knowledge 只读检索工具。
- 题型：结构化表达支持 STAR、PREP 和结构拼图；业务 Sense 支持决策推演和 Fermi 估算。每类题支持有骨架、关键词提示和限时脱稿三档难度。
- 状态与事实来源：`coaching_sessions`、`coaching_turns`；固定同题两次作答，回答使用客户端幂等键，并保存作答序号和耗时。
- 不变量：首次评价只指出最多两个优先缺口；第二次拆分评价当前回答和 V1/V2 对比。评价只使用用户连续原句，无法对齐则降级为证据不足；模型只读工具，保存与状态更新由应用服务执行。
- 业务事实：场景带版本与 `virtual/curated` 来源标签；当前内置决策场景使用教学虚拟数据，不能当作真实公司或行业事实。
- 失败表现：会话归属、状态、模型结构和工具调用均校验；工具调用另存脱敏审计。

### 模拟面试

- 入口：`/setup` -> `/interview?session=...` -> `/report?session=...`。
- 调用链：材料解析/结构化提取 -> 三角 RAG -> DeepSeek 生成计划 -> PostgreSQL 会话 -> 多轮回答与面试官决策 -> 证据化报告。
- Coding：算法题进入当前问题后自动打开右侧 Coding Board，用户可收起或重开；代码版本和公开测试运行记录经 BFF 进入 `InterviewCodingService`，本地由受限 Python 3.12 Docker 执行器运行。
- 状态与事实来源：面试会话、轮次、代码快照、运行结果和报告在 PostgreSQL；浏览器缓存只用于未提交步骤恢复。报告生成时复制最终代码、复杂度说明和运行摘要，后续编辑不会改变历史报告。
- 语音：短时 HMAC 票据连接 FastAPI WebSocket；当前讯飞 IAT/TTS 在 Provider 后，后续可替换豆包。
- 失败表现：登录、材料归属、会话状态、票据来源、回答幂等、代码版本冲突、运行次数与沙箱不可用均有明确失败路径。

### 成长档案

- 入口：`/history`、`/profile`。
- 调用链：BFF -> 历史/画像 API -> 面试报告与专项训练回答证据聚合。
- 边界：模拟面试报告形成 K 线和技术能力矩阵；专项训练以 1-5 级证据独立聚合，不能混成同一量尺。
- 报告异议：用户可请求 DeepSeek 独立复核单项评分，或明确排除出长期技能矩阵；原报告不改写，复核结论、理由、模型和 Prompt 版本单独留痕。`uncertain` 视为证据不足，不进入技能画像。
- 复训：面试弱项回到 `/setup`；表达与业务弱项回到对应 `/training/new`。专项训练按独立会话聚合证据，同一维度连续三次达到有效标准才标记稳定掌握，并生成连续训练天数、推荐难度和下一次 10 分钟计划。

## RAG 与 Agent

- 三角 RAG：`candidate` 表示候选人事实，`job` 表示岗位上下文，`knowledge` 表示题库和已审核公共知识；事实核验只允许检索 `curated_knowledge_pack`。
- 索引：规范化 -> 质量告警 -> 标题/段落/代码块感知切块 -> 豆包 1024 维向量 -> pgvector。
- 检索：向量、PostgreSQL 全文检索、trigram 和精确子串并行候选 -> 独立阈值 -> RRF 融合 -> 带来源证据。
- Verifier：报告阶段提取最多 3 条可验证主张 -> 检索已审核知识包 -> DeepSeek 批量输出 `supported / contradicted / uncertain` -> 校验引用编号和置信度 -> 保存版本化 citation。无证据或低于 0.8 的冲突自动为 `uncertain`。
- Agent：界面统一使用“AI 面试教练”身份；内部的 Coaching Agent 负责结构化表达与业务 Sense，Career Planning Agent 只生成周计划草稿。LangGraph/模型负责有限决策，不替代数据库状态，也不能直接写正式计划。
- Tools：三类只读检索工具；服务端注入用户身份，包含白名单、超时、预算、结果上限和脱敏审计。
- Skills：运行时只先加载 metadata，需要训练时再加载 `SKILL.md` 与 rubric；结构化表达教练和业务 Sense 教练均为 2.0.0，Skill、Prompt 和模型版本随会话保存。
- 资料题库：导入后保存规范化文本和文件版本，语义分片覆盖生成题目；每道题保留原文证据，资料级覆盖率和未覆盖提醒可在学习中心查看。同内容导入去重，用户可删除或基于旧版本重新生成。
- 训练来源：学习中心可选择一道个人题进入结构化表达训练；应用层先校验题目归属，再把题目、框架和原文证据传给 Agent。STAR 资料题固定进入 STAR，其他资料题进入 PREP。
- 求职记忆：`career_profiles`、`weekly_plans` 和 `weekly_plan_items` 是用户确认后的长期结构化状态；Agent 草稿保存在短期 `career_plan_drafts`，通过日期、预算、题目归属和每日上限校验后仍需用户确认。周计划按日期和早/中/晚/灵活时段展示，可联动个人题库并从今日计划直达题目或专项训练；月历、提醒和外部日历同步暂不实现。
- Skill 评测：`api/evaluations/coaching_skill_cases.json` 固定数据集与 Skill 版本，`api/scripts/evaluate_coaching_skills.py` 使用测试账号令牌运行在线评测并输出通过率、失败原因和延迟。
- MCP：当前没有第二个真实消费者，暂不封装，避免增加无收益的协议层。

## 关键文件

| 文件 | 为什么存在 | 上游 | 下游 |
|---|---|---|---|
| `web/src/components/site-header.tsx` | 三个一级入口与当前导航状态 | App layouts | 学习、训练、成长路由 |
| `api/src/interview_copilot/application/interview_planning.py` | 模拟面试材料、三角检索和计划生成 | Interview API | RAG、DeepSeek、session records |
| `web/src/features/interview-room/coding-board-drawer.tsx` | 面试内算法题编辑、版本保存和运行反馈 | Interview room | Coding BFF routes、Monaco |
| `api/src/interview_copilot/application/coding.py` | Coding 会话归属、版本冲突、幂等和运行额度 | Coding API | coding records、sandbox executor |
| `api/src/interview_copilot/sandbox/docker_python.py` | 本地 Python 3.12 受限执行 | Coding service | Docker CLI |
| `api/src/interview_copilot/application/coaching.py` | 专项训练事务、幂等和状态不变量 | Coaching API | Agent、coaching tables |
| `api/src/interview_copilot/application/agent/coach.py` | 有界训练图与 Skill 激活 | Coaching service | DeepSeek client、Skill registry |
| `api/src/interview_copilot/application/agent/career_planner.py` | 受约束的周计划草稿生成与结构化输出 | Career service | DeepSeek client、Career Planning Skill |
| `api/src/interview_copilot/application/agent/tools.py` | Tool schema、执行限制和审计事件 | Agent client | retrieval services、audit sink |
| `api/src/interview_copilot/application/question_workflows.py` | 资料版本、语义覆盖生成、原文证据与题库问答 | Questions API | parser、RAG、question provider |
| `api/src/interview_copilot/application/career.py` | 用户确认的求职画像与周计划事务 | Career API | career tables |
| `api/src/interview_copilot/application/retrieval/search.py` | 混合候选、阈值和 RRF | Tools、题库、面试计划 | pgvector/full-text repository |
| `api/src/interview_copilot/application/ability_profile.py` | 两类训练证据的独立聚合 | Profile API | reports、coaching records |
| `api/src/interview_copilot/providers/deepseek_report_review.py` | 单项评分的独立证据复核 | Report review API | DeepSeek JSON Output、review records |

## 实现状态

- 已从代码确认：账号密码登录、Redis 登录失败限流、证据化题库导入/版本/覆盖率/检索问答、三角 RAG、模拟面试状态与报告、报告异议/独立复核/画像排除、资料题结构化重答、两类专项训练、用户确认的求职画像与周计划、语音转写基线、成长档案、能力画像、工具审计、公共知识包和版本化评测。
- 登录限流按规范化账号标识的 SHA-256 摘要计数，默认 15 分钟内 8 次失败后限制；成功登录清除计数。Redis 故障时记录结构化安全降级日志并放行，避免认证服务整体不可用。
- 已真实核验：DeepSeek Tool Calling/JSON、报告单项复核、报告 Verifier（QA 场次真实得到 1 条 `supported` 与 1 条 citation）、豆包 Embedding、PostgreSQL/pgvector、讯飞 IAT/TTS；检索评测集 v2 包含正例、脏查询、易混淆和无答案共 16 例，结果为 `Recall@5=1.0`、`MRR=0.896`、Top-1 `0.833`、无答案准确率 `1.0`。
- 已真实核验：结构化表达 STAR 与业务 Sense 决策推演均完成同题两次作答；结构热身、倒计时、原句证据、最多两个缺口、业务新增约束、V1/V2 对比、证据不足降级、下一次 10 分钟计划和移动端布局均通过浏览器验收。
- 已实现：System Design Board 的结构化节点、连线、拖动、删除、自动保存、乐观版本控制与冲突处理；白板快照可按账号和面试会话持久化。
- 已实现：报告生成读取面试会话最新白板快照，保存报告级结构化副本，并在报告页提供只读回放；白板不会因后续编辑而改变历史报告。
- 已实现：账号数据导出包含报告关联的白板快照；账号删除沿面试会话级联清理白板数据。
- 已实现：题库学习状态包含间隔复习调度（复习间隔、连续掌握次数、上次复习和到期时间），仍以 `user_question_progress` 为唯一事实来源。
- 已实现：题库提供账号级“待复习”入口，按到期时间返回公共题目或本人题目，并复用原有题目详情和面试选择流程。
- 已实现：API/Web 生产镜像、Next standalone 构建、非 root 运行、生产 Compose 内部网络、启动迁移、存活/就绪检查和环境变量边界；镜像已真实构建，生产依赖安全审计为 0 漏洞。
- 已实现：FastAPI 请求 ID、结构化访问日志和统一错误关联；日志不记录查询参数或正文，校验错误不回显不可信原始输入。
- 已实现：PostgreSQL custom-format 备份与显式确认恢复脚本；真实备份已通过 `pg_restore --list` 验证，外部备份目标和保留周期仍待服务器环境确定。
- 已实现：模拟面试和专项训练语音票据一次性消费；Redis 原子阻止重放，Redis 故障时安全拒绝连接，逻辑与讯飞/豆包 Provider 解耦。
- 已实现：模拟面试算法题自动弹出 Coding Board，支持 Monaco 编辑、版本化快照、复杂度说明、公开测试运行、收起重开、报告证据和账号导出；本地 Python 3.12 Docker 沙箱关闭网络并限制 CPU、内存、进程、时间、输出和文件系统权限。
- 尚未实现：生产独立代码执行服务、生产反向代理/外部备份同步/日志告警、豆包实时语音替换。
- 当前明确不做：数字人、邮箱验证、密码找回、原文件对象存储、通用 MCP；出现真实需求或第二个消费者时重新评估。

## 下一步只做什么

下一项：在取得当前官方接口、音色和额度后评测并替换豆包实时语音 Provider。功能与本地验收完成前不继续生产部署；生产独立代码执行服务与反向代理一起留到部署阶段确定。
